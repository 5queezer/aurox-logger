# Aurox Signal Logger

Logs the Aurox signals to a database.

## Configuration
Copy `.env.sample` to `.env` and set environment variables `DATABASE_URL` and `WEBHOOK_PASSWORD`

## Run
```
docker-compose up -d
```
Enjoy!